# Core API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
pydantic-settings>=2.6.0
python-dotenv>=1.0.1
httpx>=0.27.0

# Document extraction
pymupdf>=1.24.0
python-docx>=1.1.2

# Persian NLP (POS for context-aware lexicon). Install on Python 3.11/3.12:
#   pip install hazm>=0.7.0
# Without hazm, lexicon lookup still works with null context tags.

# Audio helpers (format conversion / Grok multi-chunk concat)
pydub>=0.25.1
imageio-ffmpeg>=0.5.1

# Utilities
requests>=2.32.0
rich>=13.9.0
pytest>=8.0.0
click>=8.4.2,<9.0.0
